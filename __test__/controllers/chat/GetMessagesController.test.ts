import { GetMessagesController } from "src/controllers/chat";

describe("Get chat messages controller", () => {
  test("Should return last 10 messages", () => {});
  test("Should return the next 10 messages", () => {});
  test("Should return last 10 messages inspite of a negative messages offset", () => {});
  test("Should return an empty array due to requesting the messages that don't exist", () => {});
});
